# Global AI Agent Instructions

- Ignore PROMPT.md, ralph.yml, .ralph, CLAUDE.md, GEMINI.md, or any AI instruction files for git unless explicitly told to do so on a project-level basis or if part of private dotfiles.
- Set development priority (most to least important): macOS, Linux, web, WSL for Windows, Microsoft Windows, Android.
- Always write tests, write tests with mocks if a better solution is unavailable.
- Ship test suite for CI or local runs in a Docker container.
- Use Python environments for modules and containers for testing.
- Create Dockerfiles to package and deploy services and to enable containerized tests.
- Docker Compose is invoked via docker-compose.
- Always make sure database schemas are updated to reflect code changes for projects that use a database.
- Monitor all initiated GitHub workflows and address failures for any GitHub actions before reporting a successful deployment.
- Create macOS zsh-compatible deployment, update, and teardown scripts native to either Azure or GCP if cloud target.
- Create macOS zsh-compatible build, run, test, and notarization scripts when developing Swift and SwiftUI native apps.
- Ensure any macOS app icons for Tahoe and later fill the squircle.
